# Animation improvement plans

Generated by `improve-animations`, stamped at commit `2d44475`. These are
the top 5 findings by leverage from the full 8-category audit (13 findings
surfaced total; the remaining 8 are lower-leverage and not yet planned —
ask for them if wanted).

| # | Plan | Severity | Category | Status |
| --- | --- | --- | --- | --- |
| 001 | [Fix scale(0) entrance on engine icon marks](001-engine-cards-scale-zero.md) | HIGH | Physicality & origin | DONE |
| 002 | [Gate smooth anchor scrolling behind prefers-reduced-motion](002-hash-scroll-reduced-motion.md) | HIGH | Accessibility | DONE |
| 003 | [Transition colors on theme toggle instead of snapping](003-theme-toggle-color-transition.md) | MEDIUM | Missed opportunity | DONE |
| 004 | [Fix backwards hover asymmetry on engine cards](004-engine-cards-hover-asymmetry.md) | MEDIUM | Interruptibility | DONE |
| 005 | [Stop replaying dish-list reveal on every configurator click](005-plan-configurator-dish-reveal.md) | HIGH | Purpose/frequency, Interruptibility, Easing | DONE |

## Recommended execution order

001 → 002 → 003 → 004 → 005, in listed order. All five are independent —
no plan depends on another's changes — but 001 and 004 touch the same file
(`components/EngineCards.tsx`) in non-overlapping regions (icon-mark scale
vs. hover-handler durations), so running them back-to-back avoids
re-reading a file that changed twice in one sitting. 005 is the largest
diff (new ref, comparison logic, import change) — save it for last so a
mid-execution drift check has the least prior churn to account for.

## Dependencies

None. Each plan is self-contained and can be executed and reviewed in
isolation.

## Not yet planned (surfaced in the audit, lower leverage)

- Nav.tsx active-section marker animates `width` (Performance) — components/Nav.tsx:118-125
- Nav.tsx mobile menu items hard-reset via `fromTo` on reopen (Interruptibility) — components/Nav.tsx:161-167
- Nav.tsx mobile menu animates `height` (Performance) — components/Nav.tsx:152-157
- Problem.tsx collapse toggle animates layout properties + no reduced-motion gate (Performance + Accessibility) — components/Problem.tsx:54-59
- Stray `power2.out` vs. shared `EASE` token across 6+ call sites (Cohesion) — components/Nav.tsx:61,156,165; components/ui/AnimatedNumber.tsx:57; components/EngineCards.tsx:92,101; components/SectionDivider.tsx:32
- Missing `:active` press feedback on PlanConfigurator toggle buttons, Nav hamburger, ThemeToggle (Physicality) — components/PlanConfigurator.tsx:95-133; components/Nav.tsx:238-246; components/ui/ThemeToggle.tsx:59-65
- RecipeAssembly.tsx caption crossfade double-exposes with no blur mask (Cohesion) — components/RecipeAssembly.tsx:156-161
- JourneyBand.tsx route-node dots enter from `scale: 0` (Physicality) — components/JourneyBand.tsx:82-92

Run `/improve-animations plan <description>` for any one of these, or
re-run the full audit's `reconcile` mode later to check plans against
drifted code.
